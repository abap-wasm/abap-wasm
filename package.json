{
  "name": "wabap-wasm",
  "version": "1.0.0",
  "private": true,
  "description": "test",
  "scripts": {
    "downport": "abaplint --fix abaplint_downport.json",
    "unit": "rm -rf output && abap_transpile && echo RUNNING && node output/index.js",
    "test": "npm run downport && npm run unit"
  },
  "author": "Lars Hvam Petersen",
  "license": "MIT",
  "dependencies": {
    "@abaplint/runtime": "^0.5.31",
    "@abaplint/transpiler-cli": "^0.5.31"
  }
}
